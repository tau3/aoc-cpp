#ifndef INCLUDE_GAUSS
#define INCLUDE_GAUSS

#include <vector>

namespace Gauss {

using num_t = float;

std::vector<num_t> gauss(std::vector<std::vector<num_t>> &matrix);

} // namespace Gauss

#endif
