#include <deque>
#include <string>
#include <vector>

int solve(const std::vector<std::string> &input);

int count_arrangements(const std::string &springs,
                       const std::deque<size_t> &groups);
